# User authentication
This folder was originally user to run user authentication independently, before oauth was integrated with the rest of the application. This folder (oauth2.0) and its contents are deprecated.
